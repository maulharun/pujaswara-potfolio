/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  eslint: {
    // Consider removing this line temporarily
    // ignoreDuringBuilds: true,
  },
  typescript: {
    // Consider removing this line temporarily
    // ignoreBuildErrors: true,
  },
};

export default nextConfig;